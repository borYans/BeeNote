<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BeeNote" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/darkerBackgroundColor</item>
        <item name="colorSecondary">@color/yellowText</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.splashScreen" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/darkBackgroundColor</item>
        <item name="colorSecondary">@color/yellowText</item>
        <item name="android:colorSecondary">@color/yellowText</item>
        <item name="android:windowBackground">@drawable/splash_image</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="myActionBar" parent="Theme.MaterialComponents.Light">
    </style>
</resources>